#include once "/home/admin/dlang.net/cpi/matrix/gnu/bin/freebasic/cpmi/fbit/2ddraw/clipcontrols.bi"
#include once "/home/admin/dlang.net/cpi/matrix/gnu/bin/freebasic/cpmi/fbit/2ddraw/hasdc.bi"

'' point interact public reference count states caches equilibrium pl and pt ideas to controls path
'' form the point div key of database about the points of wallet to defense force policy equilibrium
'' pragma disput about risk of hash or implotion details of counts view list items
declare function ButtonBugget cdecl alias "ButtonBudget" (byref button as integer, byref Budget as integer ) as integer

#ifndef defined(_FB_BUTTON_BUDGET_)

#define key output: x86 -gen gcc:     11491
#define key output: x86 -gen gcc lto  11253
#define key output: x86 -gen gas:     23764
#define key output: x64 -gen gcc:     10916
#define key output: x64 -gen gcc lto: 10609 
#define key output: x64 -gen gcc lto: 10126  (compiled with -O3)
#define key output: x64 -gen gas64:   24154

#elseif defined(_FB_BUTTON_)

const gcc = 11491
const gcclto = 11253
const gas = 23764
const gccgas = 10916
const gccgaslto = 10609
const gccltogas = 10126
const gas64 = 24154

#elseif defined(_FB_BUDGET_POPUPMENU_)

type NumType as integer

function RandInt () as NumType
    return RND * &h3fff
end function

dim as NumType a,b,c
a = RandInt() '' redes social private and public
b = RandInt() '' redes social private and public
c = a * b '' redes social private and public

?c '' wait redes social private and public

'' redes social private and public getter x86 gcc gas lto
asm
   call RANDINT
   mov -72[rbp], rax #Optim 2
   
   call RANDINT
   mov -80[rbp], rax #Optim 2
   
   mov r11, QWORD PTR -72[rbp]
   imul r11, QWORD PTR -80[rbp]
   mov -88[rbp], r11
   xor ecx, ecx
   mov rdx, -88[rbp]
   mov r8d, 1
   call fb_PrintLongint
end asm

'' happen redes social private and public x86 gcc lto
asm
     call RANDINT
   mov -64[rbp], eax #Optim 16
   
   call RANDINT
   mov -68[rbp], eax #Optim 16
   
   movsxd r11, DWORD PTR -64[rbp]
   movsxd r10, DWORD PTR -68[rbp]
   imul r11, r10
   
   mov -72[rbp], r11d #Optim 2
   xor ecx, ecx
   mov edx, -72[rbp]
   mov r8d, 1
   call fb_PrintInt
end type

'' lockstreet elements x86 gcc gas lto
asm
   call RANDINT
   mov -64[rbp], eax #Optim 16
   
   call RANDINT
   mov -68[rbp], eax #Optim 16
   
   movsxd r11, DWORD PTR -64[rbp]
   movsxd r10, DWORD PTR -68[rbp]
   imul r11, r10
   
   mov -72[rbp], r11d #Optim 2
   xor ecx, ecx
   mov edx, -72[rbp]
   mov r8d, 1
   call fb_PrintInt
end asm

'' magazine news jorunal office layout pages
asm
  	int32 vr$4 = RANDINT(  );
	A$0 = vr$4;
	int32 vr$5 = RANDINT(  );
	B$0 = vr$5;
	C$0 = (int32)(int64)(A$0 / B$0);
end asm

end      
'' 2d raw bar tools to buffer button select full pull push one URL
'' select mapping label input states button click method street path
'' mapping template ordering values to myojo click panware map lick
'' to button happy logical template mapper check click select ware
'' document 2d draw
#include once "picturebox.bi"

if Box.MiddleCenter = "ButtonPatternCenter" then
   EventBox(Box.MiddleCenter)
end if

declare function EventTables (byref Buffer as ButtonAlignment) as ButtonAlignment

dim Buffer as ButtonAlignment

if Buffer = EventTicket then
   EventTicket(Fantons.dialogEventMathExp)
else
   Buffer.MiddleLeft = 0  '' push pull one layout
end if

if Buffer = EventTicket then
   Eventticket(Fantons.dialogEventMathExp)
else
   Buffer.MiddleRight = 0 '' push pull one layout  
end if

end

declare function EventPipeEffectBox (byref Pipe as CheckBoxState) as CheckBoxState

dim Pipe as CheckBoxState

if Pipe = CheckBoxState then
   Pipe.Checked = "Button Test"
end if

end